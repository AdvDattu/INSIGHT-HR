name: Build Emergent Mobile App (APK)

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  # Limit Node.js to 4GB of RAM so it doesn't crash the server
  NODE_OPTIONS: "--max-old-space-size=4096"

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Generate Android folder (Expo Prebuild)
        run: |
          npx expo prebuild --platform android --clean

      - name: Restrict Gradle Memory Usage
        # This prevents the exit code 254 memory crash by capping RAM and disabling parallel builds
        run: |
          echo "org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=512m" >> android/gradle.properties
          echo "org.gradle.daemon=false" >> android/gradle.properties
          echo "org.gradle.parallel=false" >> android/gradle.properties

      - name: Build Android APK
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assembleRelease
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: INSIGHT-HR-App
          path: android/app/build/outputs/apk/release/app-release.apk
